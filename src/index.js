import React from 'react'; // useState is imported so we can use the hook feature : state for functional component
import ReactDOM from 'react-dom';
import './app.css';
import App from './App';


ReactDOM.render(<App/>,document.getElementById('root'));

